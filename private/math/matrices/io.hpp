#pragma once
#include <iostream>
#include <xme/math/forward.hpp>

namespace xme {
/*
    template<typename T, std::size_t Cols, std::size_t Rows>
    std::ostream& operator<<(std::ostream& os, const Matrix<T, Cols, Rows>& m) {
        os << "Matrix(";
        os << ')';
        return os;
    }
*/
}